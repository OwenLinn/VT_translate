[project]
name = "yt-live-translator"
version = "0.1.0"
description = "Windows desktop real-time translation subtitle overlay for YouTube livestreams"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
